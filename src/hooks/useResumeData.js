/**
 * useResumeData.js
 * Convenience hook — re-exports context accessors.
 * Use this instead of importing useResume directly in components.
 */

import { useResume } from '../context/ResumeContext'
export default useResume
