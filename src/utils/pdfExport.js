/**
 * pdfExport.js
 * Handles PDF generation from the resume preview.
 * TODO: Implement using react-to-print in Phase 1.
 */

/**
 * Triggers browser print dialog targeting the resume element.
 * @param {React.RefObject} resumeRef - ref attached to the resume preview div
 */
export const downloadAsPDF = (resumeRef) => {
  // Placeholder — will be wired to react-to-print
  console.log('PDF download triggered', resumeRef)
}
